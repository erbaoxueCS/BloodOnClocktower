# Dev Plan（当前迭代整理）

## 目标与原则

- 统一 AI 玩法架构为：`玩家意图 -> 说书人裁决 -> 引擎落地`。
- 说书人未开启 AI 接管时，流程不应被系统自动快进。
- 保留规则允许的玩法多样性，策略倾向主要通过提示词与记忆摘要调优，而不是硬编码限制玩法。

## 本轮已完成改动

### 1) 夜间行动从“改写器”改为“校验器”

- 调整夜间提交逻辑：说书人层主要负责合法性校验与理由记录，不再无条件重写玩家目标。
- 重点保证：玩家夜间意图在合法前提下被保留，和实体桌游说书人裁定职责一致。

涉及文件：

- `server/src/index.ts`
- `server/src/ai/storyteller.ts`
- `server/src/game/gameEngine.ts`
- `server/src/script/troubleBrewing.ts`

### 2) 裁决层（Adjudication Layer）统一入口

- 新增/完善提名、投票、夜间行动的统一裁决与记录链路。
- 为每次动作记录来源与裁决结果（人类玩家、AI 玩家、系统兜底），便于复盘与调试。
- 暴露裁决日志查询接口，支持后续自动化评估。

涉及文件：

- `server/src/index.ts`
- `server/src/ai/storyteller.ts`
- `server/src/game/gameEngine.ts`

### 3) AI 玩家记忆从原始聊天改为“摘要优先”

- 引入玩家记忆摘要刷新流程：先用结构化摘要作为决策主上下文，再保留少量原始尾部信息做校验。
- 目标是降低上下文噪音与 token 成本，提升发言/提名/投票决策稳定性。

涉及文件：

- `server/src/ai/playerAgent.ts`
- `server/src/index.ts`

### 4) 白天流程与公开发言兜底修复

- 公开发言阶段增加显式完成标记与超时兜底，避免“无人发言但流程继续”。
- 死亡玩家允许参与讨论；提名与幽灵票限制仍由规则层控制。

涉及文件：

- `server/src/index.ts`
- `client/src/Game.tsx`

### 5) 自动化测试与指标汇总

- 新增后端一键自测入口（自动建房、托管、开局、收集结果）。
- 扩展对局摘要与聚合指标，支持快速比较不同提示词风格下的行为差异。

涉及文件：

- `server/src/index.ts`
- `server/src/ai/playerAgent.ts`

### 6) 关键边界修复：未开启 AI 说书人时禁止自动推进

- 在流程导演与 AI 托管入口增加硬门禁：仅 `aiStorytellerEnabled=true` 时允许自动导演/兜底推进。
- 修复点包括白天托管入口，确保“未接管说书人 -> 流程应卡在需说书人操作处”。

涉及文件：

- `server/src/index.ts`

## 规则一致性文档

- 新增：`RULE_CONSISTENCY_CHECKLIST.md`
- 用于跟踪与 Trouble Brewing 官方规则的一致性差距、优先级与验收标准。

## 验证建议（由你执行）

- 场景 A：不开 AI 说书人，只开 AI 玩家  
  预期：流程不会自动跑完整局，会停在需要说书人推进的节点。
- 场景 B：开启 AI 说书人与 AI 玩家  
  预期：可自动推进，并在公开发言、提名投票、夜晚结算等阶段产生可追踪日志。
- 场景 C：死亡玩家在白天  
  预期：可参与讨论；不可提名；仅可使用一次幽灵赞成票。

## 下一步建议（未完成）

- 将 `storyteller_ai` 在白天提名/投票阶段补齐为“可解释、可配置”的独立裁量策略（目前以规则引擎镜像为主）。
- 基于自动化对局结果继续扩展指标（例如发言覆盖率、有效提名率、坏人推进效率、关键角色存活关联）。
